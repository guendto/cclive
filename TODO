repo: git://repo.or.cz/cclive.git
web : http://repo.or.cz/w/cclive.git

* manual: add --regexp, --find-all
* manual: update "groups" with "sections" as done in cmdline.ggo
* add: -r, --title-regex=expr
  - use regular expression to filter video titles before using them
  for output filenames
* add: -g, --find-all
  - use repeated matching to find all occurences, like Perl's /g option
  dependon="title-regex"

  Examples:
    cclive -gr "(\\w)" URL
